# M11 luxury redesign and approved import

Status: complete on 2026-07-14 after explicit approval for a private local import.

## Interface

- Replaced the light/blue palette with the Dragon Noir system: near-black surfaces,
  crimson focus and action states, warm neutral typography, depth, and restrained grain.
- Upgraded the login, global shell, navigation, forms, media grids, lists, tables,
  dialogs, and mobile navigation without changing the Flask/Jinja architecture.
- Added one ambient signature treatment plus purposeful entrance, hover, press, focus,
  dialog, and surface transitions. Reduced-motion preferences disable choreography.
- Kept visible keyboard focus, semantic landmarks, responsive behavior, and local fonts.

## Approved local import

- Added the explicit `migrate apply --confirm-private-import` workflow.
- Imported supported movie, playback, reading, chess, and German-learning records into
  normalized models with natural-key reconciliation and legacy ID mappings.
- Archived raw datasets and chat history under ignored instance storage so unsupported
  source data remains available for future adapters.
- Merged legacy integration variables into the ignored Dragon `.env` and copied OAuth
  files to ignored `instance/secrets`; no values are printed in reports or tracked files.
- Confirmed the legacy source fingerprint was identical before and after apply.

## Verification

- 66 non-browser tests and 13 responsive browser/accessibility tests passed.
- Application statement coverage: 85% after adding the importer.
- Ruff, compileall, diff whitespace, frontend policy, and tracked-secret scans passed.
- Live login rendering uses the new design layer with no page-level horizontal overflow.
- The value-free local import report is stored at
  `instance/migration/legacy-import-report.json`.
